# Java + Gradle Example (Test-based)

## Run

```bash
./gradlew test
```

If GitHub Packages auth is required, set:

- `GITHUB_ACTOR`
- `GITHUB_TOKEN`

Or in `~/.gradle/gradle.properties`:

- `gpr.user=...`
- `gpr.key=...`
