# No public API changes relative to apiBaselineVersion for the 2.2.0 maintenance release.
# Any future entry must identify an exact symbol, a tested contract change, and a changelog entry.
# Never add package-wide wildcards.
