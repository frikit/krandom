# Intentional public API incompatibilities relative to apiBaselineVersion.
#
# The v3 line starts with no approved break from the released 2.2.0 API, so this file is
# intentionally empty. Every future exclusion requires, in the same reviewed change:
# - an approved major-version contract in docs/development/v3-preparation-plan.md;
# - the narrowest class or member signature supported by japicmp;
# - a compile-tested migration example and rollback path;
# - a changelog entry.
#
# Never add package-wide wildcards.
