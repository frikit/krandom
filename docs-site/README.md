# docs-site

Public GitHub Pages site for kRandom Java documentation.

## Local preview (optional)

If you have Ruby + Jekyll installed:

```bash
cd docs-site
bundle exec jekyll serve
```

Otherwise rely on the `github-pages` workflow to build and deploy.
